namespace ET
{
    // Keep a dedicated source file for the shared node base so Unity's generated project
    // can reference a stable path while the main members stay in BehaviorTreeDefinitions.cs.
    public abstract partial class BTNodeData
    {
    }
}
