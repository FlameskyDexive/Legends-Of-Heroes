# AGENTS.md

- 请把 `CLAUDE.md` 视为项目权威指南，先阅读该文件了解工作流、工具链与编码规则。
- 当任务对应到 Claude Code skill 时，打开 `.claude/skills/<skill>/SKILL.md`（或指定的 Markdown）并完整遵循其中指令。
- 优先直接复用 skill 引导的脚本与命令（本仓库均需使用 PowerShell），不要自行改写流程。
- 如果缺少某个 skill 或说明不清，请先询问用户，或退回参考 `CLAUDE.md` 的通用指引。
- 请一直使用中文
- 做出决策前请先说明你的思路，好让我看到你的思路是否合理。
